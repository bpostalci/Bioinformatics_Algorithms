#include "UPGMA.h"
#include <iostream>


void buildUPGMA(double **dm, const vector<seq>& seqs, const string &outfile)
{
    for (auto it : seqs) 
    {
        cout << "title: " << it.title << "\n";
    }

    
}