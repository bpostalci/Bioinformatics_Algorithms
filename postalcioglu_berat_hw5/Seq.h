#pragma once

#include <string>

struct seq
{
public:
  std::string seq;
  std::string title;
  double score;
  // seq(const std::string &_seq, const std::string _title, double _score) : seq(_seq), title(_title), score(_score)
  // {
  // }
};